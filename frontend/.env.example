# Base URL of the backend API. Baked in at build time by Vite.
# `make dev` in ../backend runs the API on the host at :5007.
# The docker-compose "api" service (currently commented out) publishes :8080 instead.
VITE_API_URL=http://localhost:5007/api
